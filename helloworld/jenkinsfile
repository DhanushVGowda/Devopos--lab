pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh '''
                    echo "displaying the system uptime"
                    uptime
                '''
            }
        }
    }
}
